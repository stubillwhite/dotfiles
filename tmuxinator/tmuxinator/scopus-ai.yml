# /Users/white1/.config/tmuxinator/scopus-ai.yml

name: scopus-ai
root: ~/Dev/kd/butter-chicken/

windows:
  - sebright:
      layout: even-vertical
      panes:
        - genai:
            - cd kd-genai-service
            - git pull -r --autostash
        - performance:
            - cd kd-performance-genai
            - git pull -r --autostash
