# /Users/white1/.config/tmuxinator/kafka.yml

name: kafka
root: ~/Dev/recommenders/kd-recs-kafka-connectors/sutd-limf-user-preferences

windows:
  - kafka:
      layout: even-vertical
      panes:
        - docker-compose logs -f kafka
        - docker-compose logs -f kafka-connect
  - vim:
      layout: even-horizontal
      panes:
        - nvim docker-compose.yml kafka-connector/user-signals-ingestion-config.json
  - shell:
      root: ./
      layout: even-horizontal
      panes:
        - #
