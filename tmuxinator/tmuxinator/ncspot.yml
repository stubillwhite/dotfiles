name: ncspot

windows:
  - ncspot:
      layout: even-vertical
      panes:
        - ncspot:
            - ncspot
  - muti-scrobbler:
      - pushd ~/dev/my-stuff/shell-utils/multi-scrobbler && make run
