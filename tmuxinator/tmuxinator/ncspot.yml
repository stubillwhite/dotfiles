name: ncspot

windows:
  - ncspot:
      layout: even-vertical
      panes:
        - ncspot:
            - ncspot
  - muti-scrobbler:
      - echo pushd ~/dev/my-stuff/shell-utils/multi-scrobbler && make run
