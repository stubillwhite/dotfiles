# /Users/white1/.config/tmuxinator/recs-reviewer-live.yml

name: recs-reviewer-live
root: ~

windows:
  - recs-reviewer-live:
      layout: even-vertical
      panes:
        - data-pump:
            - aws-recs-prod
            - awslogs get -S -G -w --timestamp /aws/lambda/recs-rev-manuscripts-data-pump-lambda-live
        - infra:
            - aws-recs-prod
            - awslogs get -S -G -w --timestamp /aws/lambda/recs-reviewers-recommender-lambda-live
