# /Users/white1/.config/tmuxinator/recs.yml

name: recs
root: ~/Dev/recommenders

windows:
  - recs:
      layout: even-horizontal
      panes:
        - main:
            - git-repos-fetch
        - infra:
            - pushd infra
            - git-repos-fetch
