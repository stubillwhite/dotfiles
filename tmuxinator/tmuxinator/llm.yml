name: llm
root: ~/dev/my-stuff/llm-scratchpad

windows:
  - llm-scratchpad:
      layout: even-horizontal
      panes:
        - prompt:
            - vim app.py prompt.txt
        - response:
            - git ls-files | entr -s 'make run'
